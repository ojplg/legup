
#set ( $links = [ "<a href='/legup/view_report_cards'>Report Card Index</a>" ] )

#parse( "/templates/insert_nav_bar.vtl" )

<h4>Edit Report Card</h4>

<form action="/legup/save_report_card" id="save_report_card_form" class="pure-form pure-form-aligned">

    <fieldset>
        <input type="hidden" name="id" value="$!report_card.Id"/>

        <div class="pure-control-group">
            <label for="name">Name</label>
            <input type="text" name="name" value="$!report_card.Name"/>
        </div>

        <div class="pure-control-group">
            <label for="session">Session</label>
            <input type="text" name="session" value="$!report_card.SessionNumber"/>
        </div>

        <h5>Factors</h5>
        <table class="pure-table">
            <tr>
                <th>Bill Chamber</th>
                <th>Bill Number</th>
                <th>Short Description</th>
                <th>Session</th>
                <th>Desired Vote</th>
            </tr>

            #foreach ($pair in ${factor_settings.entrySet()})
                #set ( $bill = $pair.Key )
                #set ( $setting = $pair.Value )
                <tr>
                    <td>$bill.Chamber</td>
                    <td>$bill.Number</td>
                    <td>$bill.ShortDescription</td>
                    <td>$bill.Session</td>
                    <td>
                        <select name="bill_vote_side_$bill.id" form="save_report_card_form">
                            <option value="Unselect"></option>
                            <option value="Yes" #if (${setting.equals("Y")}) selected="selected" #end>Yes</option>
                            <option value="No" #if (${setting.equals("N")}) selected="selected" #end>No</option>
                        </select>
                    </td>
                </tr>
            #end
        </table>

        <div class="pure-g">

            <div class="pure-u-1-2">
                <h5>House</h5>
                <table>
                    #foreach ( $pair in ${selectedHouse.entrySet()})
                        #set ( $legislator = $pair.Key )
                        <tr>
                            <td>$legislator.Display</td>
                            <td>
                                <input type="checkbox" name="legislator_$legislator.id" #if ( $pair.Value ) checked #end >
                            </td>
                        </tr>
                    #end
                </table>
            </div>

            <div class="pure-u-1-2">
                <h5>Senate</h5>
                <table>
                    #foreach ( $pair in ${selectedSenate.entrySet()})
                        #set ( $legislator = $pair.Key )
                        <tr>
                            <td>$legislator.Display</td>
                            <td>
                                <input type="checkbox" name="legislator_$legislator.id" #if ( $pair.Value ) checked #end >
                            </td>
                        </tr>
                    #end
                </table>
            </div>

        </div>

        <input type="submit" value="Save"/>

    <fieldset>

</form>
